# AI開発デモ 提案書
## アプリ名: 生コンAI出荷管理

---

## 1. アプリ概要

### 対象業務
生コンクリート工場における受注〜出荷管理（電話受注・事前発注の複合フロー）

### 技術スタック

| 要素 | 内容 |
|------|------|
| フレームワーク | ASP.NET Core 10 MVC |
| DB | SQLite（EF Core） |
| UI | Bootstrap 5 + Chart.js |
| 認証 | ASP.NET Core Identity |
| レポート | ClosedXML.Report（Excel出力） |

---

## 2. 画面構成（メニュー）

```
生コンAI出荷管理
│
├── ダッシュボード          ← 出荷状況リアルタイム表示
│
├── 受注管理
│   ├── 受注入力            ← 日次：電話受注・事前発注の登録
│   ├── 受注一覧
│   └── 配車計画            ← 日次：ミキサー車の割り当て
│
├── 出荷管理
│   ├── 出荷指示            ← 日次：出荷確定・納品書印刷
│   ├── 出荷実績一覧
│   └── 週次集計レポート    ← 週次：得意先別・配合別集計
│
├── 請求管理
│   └── 月次請求処理        ← 月次：請求書発行・締め処理
│
└── マスタ管理
    ├── 得意先マスタ
    ├── 配合マスタ          ← 生コン固有（強度・スランプ・骨材等）
    ├── 車両マスタ          ← ミキサー車・ドライバー管理
    └── 工場マスタ
```

---

## 3. 事前に用意しておくコードの状態

```
「動いているが、荒削りなコード」として用意する
```

**意図的に不完全にしておく箇所**
- バリデーションが最低限（必須チェックのみ）
- テストコード 0件
- ドキュメント・コメントなし
- エラーハンドリングが雑（例外をそのまま表示）
- セキュリティ考慮なし（@Html.Raw の多用など）
- パフォーマンス無考慮（N+1クエリあり）

---

## 4. 並列デモ 4タスク（メイン）

発表中にターミナルを4分割して同時実行。
「今、4つの作業がAIで並行しています」と実況しながら進める。

---

### Task A: 機能追加
**「出荷実績のExcel出力と月次請求書を追加して」**

```
【投入プロンプト】
ShipmentsControllerと出荷関連のモデルを確認して、
ClosedXML.Reportを使った以下の出力機能を追加してください。

1. 出荷実績一覧のExcelダウンロード
   - ボタンを一覧画面に追加
   - ファイル名: shipments_YYYYMMDD.xlsx
   - Excelテンプレート（Templates/ShipmentList.xlsx）を使用
   - 得意先名・配合名・出荷量・出荷時刻・単価・金額を出力
   - 合計行を自動挿入

2. 月次請求書のExcel出力
   - テンプレート（Templates/Invoice.xlsx）を使用
   - 得意先ごとに1シート生成、当月の出荷明細＋合計を記載
   - ファイル名: invoice_得意先名_YYYYMM.xlsx
```

**見せ場**: AIが既存コードを読んでから実装 → 「ゼロから指示しなくても既存コードに合わせてくれる」

---

### Task B: テスト自動生成
**「受注入力のテストを書いて」**

```
【投入プロンプト】
OrdersControllerのCreate、Edit、Deleteアクションに対して
xUnitを使ったユニットテストを生成してください。

- Moqを使ってDBコンテキストをモック化する
- 以下のケースを含める:
  ・正常系: 有効な受注データで登録成功
  ・異常系: 出荷予定日が過去日付の場合はエラー
  ・異常系: 数量が0以下の場合はバリデーションエラー
  ・境界値: 配合マスタに存在しないコードを指定した場合
- テストメソッド名: 「メソッド名_状態_期待結果」の命名規則
- テストプロジェクトがなければ新規作成も含めて
```

**見せ場**: 「テストを書く時間がない」あるある → AIが数分で網羅的に生成

---

### Task C: セキュリティ診断 + 修正
**「セキュリティ問題を見つけて直して」**

```
【投入プロンプト】
この生コン出荷管理システムのセキュリティを
OWASP Top 10の観点でレビューしてください。

問題を発見したら重要度（高/中/低）と修正コードをセットで提示し、
修正まで実施してください。特に以下を重点確認:

- SQLインジェクション（EF CoreのRawSqlの使い方）
- XSS（RazorビューでのHTML出力）
- CSRF対策（受注入力・出荷確定フォーム）
- 認証・認可の漏れ（マスタ管理画面への未認証アクセス）
- 不適切なエラー情報の露出
```

**見せ場**: 専門家不要でセキュリティ診断 → 「コンサルに頼むと数十万円のレビューが数分で」

---

### Task D: ドキュメント + コード品質改善
**「ドキュメントを整備してコードを読みやすくして」**

```
【投入プロンプト】
このプロジェクトのコードを読んで以下を生成・改善してください:

1. README.md の作成
   - システム概要、対象業務フロー図（テキストベース）
   - 環境構築手順（dotnet restore, DB migration, 初期データ投入）
   - 画面一覧と各画面の説明

2. 主要Controllerへの XMLドキュメントコメント追加
   - Swaggerで表示できる形式（<summary>/<param>/<returns>）

3. N+1クエリの検出と修正
   - 出荷実績一覧で発生しているN+1を Include() で解消
```

**見せ場**: 誰もやりたがない作業 + パフォーマンス改善が同時に → 参加者の「あるある」に刺さる

---

## 5. 補足デモ（時間があれば）

### バグ修正デモ（リアルタイム）
事前に仕込んだバグを画面で見せてから投入する

```
【投入プロンプト】
受注入力画面で、出荷予定日を空欄のまま登録すると
「Object reference not set to an instance of an object」が表示されます。
原因を特定して、ユーザーにわかりやすいバリデーションメッセージに修正してください。
また、同様の問題が他のフォームにないか合わせて確認してください。
```

---

## 6. 発表の構成案（40分想定）

| 時間 | 内容 |
|------|------|
| 5分 | 「AIと開発するとは？」概念説明（チャットAIとの違い） |
| 5分 | 生コンAI出荷管理システムの紹介（画面デモ） |
| 15分 | **並列4タスクデモ（メイン）** |
| 5分 | 結果確認・テスト実行・動作確認 |
| 5分 | 「工数・コスト・品質」の変化を数字で語る |
| 5分 | Q&A |

---

## 7. 「驚かせる」ための演出ポイント

1. **事前に時間を計測しておく**
   「この受注入力のテスト、普通なら1〜2日かかります」と言ってから開始

2. **4画面同時進行**
   ターミナルを4分割してリアルタイムで4つのAIエージェントが動く様子を見せる

3. **AIがコードを読む場面を見せる**
   「何も説明していないのに業務コードを理解して作業している」

4. **生成したテストを実際に実行**
   `dotnet test` でグリーンになる瞬間を見せる

5. **「プロンプトはこれだけ」**
   数行の日本語指示 → 数百行のコードという対比を見せる

6. **セキュリティ結果の重要度一覧**
   「高：3件、中：5件」のような診断レポートが出ると説得力大

---

## 8. 事前準備チェックリスト

- [ ] ASP.NET Core 10 プロジェクト作成・動作確認
- [ ] SQLite DBファイル作成・EF Core マイグレーション適用済み
- [ ] 「改善余地あり」な状態のコードをgit管理
- [ ] Excelテンプレートファイル（Templates/）を用意
- [ ] 初期データ投入（得意先・配合・車両マスタのサンプル）
- [ ] 4タスクのプロンプトをテキストファイルに準備（コピペできる状態）
- [ ] ターミナル4分割の画面レイアウトを事前設定
- [ ] デモ環境のネット接続・APIキー確認
- [ ] 「普通ならX時間かかる」の根拠数字を準備
- [ ] テスト実行が通る状態を事前リハーサルで確認
